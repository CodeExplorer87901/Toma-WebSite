# Настройка Firebase для синхронизации товаров

## Проблема
Товары сохраняются только локально в браузере (localStorage), поэтому не синхронизируются между устройствами.

## Решение
Используем Firebase Firestore для хранения данных в облаке.

---

## Шаг 1: Создание Firebase проекта

1. Открой [Firebase Console](https://console.firebase.google.com/)
2. Нажми **"Создать проект"** / **"Add project"**
3. Введи название проекта (например, `outfit-store`)
4. Отключи Google Analytics (не обязательно для этого проекта)
5. Нажми **"Создать проект"**

---

## Шаг 2: Создание Web приложения

1. На главной странице проекта нажми на иконку **`</>`** (Web)
2. Введи название приложения (например, `outfit-catalog`)
3. **НЕ** ставь галочку "Firebase Hosting"
4. Нажми **"Зарегистрировать приложение"**
5. Скопируй конфигурацию `firebaseConfig` - она будет выглядеть примерно так:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "outfit-store-xxxxx.firebaseapp.com",
  projectId: "outfit-store-xxxxx",
  storageBucket: "outfit-store-xxxxx.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:xxxxxxxxxxxxx"
};
```

---

## Шаг 3: Настройка Firestore Database

1. В боковом меню выбери **"Firestore Database"**
2. Нажми **"Создать базу данных"** / **"Create database"**
3. Выбери режим **"Start in test mode"** (для разработки)
4. Выбери регион (например, `europe-west` для Европы)
5. Нажми **"Включить"** / **"Enable"**

### Настройка правил безопасности

После создания базы данных, перейди на вкладку **"Rules"** и замени правила на:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Все могут читать товары и статус магазина
    match /products/{product} {
      allow read: if true;
      allow write: if true; // В production лучше добавить аутентификацию
    }
    
    match /storeStatus/{status} {
      allow read: if true;
      allow write: if true; // В production лучше добавить аутентификацию
    }
  }
}
```

**Важно:** Для production-версии нужно настроить Firebase Authentication и ограничить права записи только для аутентифицированных администраторов.

Нажми **"Publish"** / **"Опубликовать"**

---

## Шаг 4: Настройка локального проекта

1. Создай файл `.env` в корне проекта (скопируй `.env.example`)
2. Вставь свои значения из Firebase Console:

```env
VITE_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_FIREBASE_AUTH_DOMAIN=outfit-store-xxxxx.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=outfit-store-xxxxx
VITE_FIREBASE_STORAGE_BUCKET=outfit-store-xxxxx.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:xxxxxxxxxxxxx
```

3. **ВАЖНО:** Добавь `.env` в `.gitignore`, чтобы не выкладывать ключи в GitHub

---

## Шаг 5: Запуск приложения

1. Перезапусти сервер разработки:
```bash
npm run dev
```

2. Зайди на страницу администратора и добавь товар
3. Открой сайт с другого устройства - товар должен отображаться!

---

## Шаг 6: Деплой на GitHub Pages

Для того чтобы переменные окружения работали на GitHub Pages:

1. Зайди в настройки репозитория на GitHub
2. Перейди в **Settings** → **Secrets and variables** → **Actions**
3. Добавь все переменные окружения как секреты:
   - `VITE_FIREBASE_API_KEY`
   - `VITE_FIREBASE_AUTH_DOMAIN`
   - `VITE_FIREBASE_PROJECT_ID`
   - `VITE_FIREBASE_STORAGE_BUCKET`
   - `VITE_FIREBASE_MESSAGING_SENDER_ID`
   - `VITE_FIREBASE_APP_ID`

4. Создай файл `.github/workflows/deploy.yml` (я создам его для тебя)

---

## Проверка работы

1. Открой сайт на ноутбуке
2. Войди как кассир (admin / 1234)
3. Добавь новый товар
4. Открой сайт на телефоне
5. Товар должен появиться автоматически! ✨

---

## Troubleshooting

### Товары не появляются на другом устройстве

1. Проверь консоль браузера на ошибки
2. Убедись что правила Firestore разрешают чтение/запись
3. Проверь что все переменные окружения заполнены
4. Очисти кэш браузера

### Firebase errors в консоли

- Проверь что все ключи из `.env` правильно скопированы
- Убедись что Firestore Database создана и включена
- Проверь правила безопасности

---

## Безопасность (для production)

Для production-версии рекомендую:

1. Настроить Firebase Authentication
2. Изменить правила Firestore чтобы только админы могли изменять товары
3. Использовать Firebase Security Rules для валидации данных
4. Включить App Check для защиты от ботов

Свяжись со мной если нужна помощь с настройкой! 🚀
